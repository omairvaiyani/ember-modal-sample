import modal from 'ember-modal-sample/utils/modal';
import { module, test } from 'qunit';

module('Unit | Utility | modal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = modal();
  assert.ok(result);
});
